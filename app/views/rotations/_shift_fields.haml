.nested-fields
  / .small
  /   = f.count
  - unless f.object.volunteer.nil?
    %div.assigned_shift
      %span
        Shift Assigned to:
      %h5
        - user = f.object.volunteer
        = link_to user.full_name.capitalize, user
      %span.remove
        = link_to_remove_association "x", f
  - else
    %div.assigned_shift.unassigned
      = f.association :volunteer,
      collection: @vols,
      label_method: :select_label

      %span.remove
        = link_to_remove_association "x", f