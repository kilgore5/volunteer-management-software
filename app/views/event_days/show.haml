/ # = render partial: "event_days/day_full", locals: {day: @event_day}
- content_for :hero_title do
  = "#{@event_day.event.name}: #{@event_day.date.strftime("%A")}, #{@event_day.date.strftime("%d/%m/%Y")}"

- day = @event_day
.row 
  .col-md-4.col-sm-6.col-xs-12.event_day.event_day
    .panel.default
      .top-key-factor
        .badge-container
          %span.badge.excellent EXCELLENT
        .centerify-icon
          %span.icon
        .event_day__title
          %h5
            Jobs
        %hr    
        .event_day__jobs
          = "Jobs assigned: #{day.jobs.count}"
          %br
          %table{class: "table table-sm"}
            %thead
              %tr
                %th
                  Job
                %th
                  Shifts
                %th
                  Shifts                  
            %tbody
              - day.jobs.each do |job|
                %tr
                  %td
                    = job.title
                  %td
                  %td
          %br
  .col-md-4.col-sm-6.col-xs-12.event_day.event_day
    .panel.default
      .top-key-factor
        .badge-container
          %span.badge.excellent EXCELLENT
        .centerify-icon
          %span.icon       
        .event_day__title
          %h5
            Volunteers
        %hr    
        .event_day__workers
          = "Volunteers needed: #{day.total_workers_needed}"
          %br     
  .col-md-4.col-sm-6.col-xs-12.event_day.event_day
    .panel.default
      .top-key-factor
        .badge-container
          %span.badge.excellent EXCELLENT
        .centerify-icon
          %span.icon
        .event_day__title
          %h5
            Shifts
        %hr    
        .event_day__workers
          = "Total Shifts: #{day.total_shifts}"
          %br                                         
        / - @event_day.event.jobs.each do |job|
        /   = job.
                                                                     


/ = link_to 'Edit', edit_event_path(@event)
/ \|
/ = link_to 'Back', events_path
