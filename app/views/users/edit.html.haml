/ Renders the header section content
- content_for :page_title do
  %h1 Manage Your Account
- content_for :page_sub_title do
  %p It's easy to manage your account.  Quickly access or update your information.

/ Renders the user's details
- content_for :profile do
  = render "user_details"

%h5
  My Applications
= render 'applications'

%h5
  Shifts
= render 'shifts'

= render 'payment_methods'

= render 'form'


= render 'users/registrations/cancel_account'

/ %hr

/ = link_to 'Show', @user
/ \|
/ = link_to 'Back', users_path
